pfc* graphs = all lowest-level FC regions and edges from M&S of interest to us

lateral* graphs = subgraph of the pfc* graph that was clustered as a single module after the first run of SA

cocomac* graph = all M&S regions and edges

mapping directory = graphs built from CoCoMac mapping queries